# runs

Active/current run output area.

During the repo transition, this directory also contains compatibility symlinks
to completed runs in `baseline_runs/` and to the DoDoMen case run in
`case_studies/dodomen/`.

New real pipeline runs may still write here by default.

Completed channel examples should eventually live in `baseline_runs/`, not as
large physical directories in `runs/`.
